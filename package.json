{
  "name": "js-toast-msg",
  "version": "1.0.5",
  "description": "Library that displays toast messages on the html screen.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*",
    "src/css/**/*"
  ],
  "scripts": {
    "esbuild": "esbuild --outdir=dist --bundle --minify-identifiers --minify-whitespace --format=cjs src/index.ts",
    "build": "esbuild --config=esbuild.config.js",
    "webpackBuild": "webpack --config webpack.config.js",
    "test": "test"
  },
  "keywords": [
    "javascript",
    "toast"
  ],
  "author": "peter yo",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.17.18",
    "esbuild-loader": "^3.0.1",
    "ts-loader": "^9.4.2",
    "typescript": "^5.0.4",
    "uglifyjs-webpack-plugin": "^2.2.0"
  },
  "dependencies": {}
}
